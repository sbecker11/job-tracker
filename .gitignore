.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.env
.env.*
!.env.example
*.db
*.db.bak*

# Gmail OAuth — never commit (prefer ~/.config/job-tracker/)
credentials.json
token.json
client_secret*.json

.DS_Store

# Generated/personal artifacts (regenerate via scripts/render_pending_actions.py
# etc.) — derived from leads.db, which is itself gitignored above.
var/
